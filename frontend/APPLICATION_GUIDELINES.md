# Frontend Guidelines

Keep credentials and secrets out of the frontend. Use accessible labels, responsive layouts, and show useful loading, error, and empty states. API calls are centralized in `js/app.js`.
